PDFs now render without any native code, so there is one download for every phone instead of four, and it is about 8 MB rather than 15. Files Gander cannot recognise offer a View as text button instead of a dead end, and large text files and logs open straight away, a few megabytes at a time. PDFs open around half a second slower than before, which is the cost of dropping the old renderer.
